import React from 'react';
import {View} from 'react-native'
import { Card, ListItem, Button, Icon } from 'react-native-elements'

export default class Card extends React.Component{
    render(){
        return(
            <Card title="PENSION LA WEA">
                <View>

                <View>
            </Card>
        )
    }
}
